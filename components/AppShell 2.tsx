"use client";
import Link from "next/link"; import { usePathname } from "next/navigation"; import { LayoutDashboard, Package, Upload, Settings, LogOut } from "lucide-react"; import { createClient } from "@/lib/supabase/client";
export default function AppShell({ children }: { children: React.ReactNode }) { const path = usePathname(); const links = [{ href: "/dashboard", label: "Dashboard", icon: LayoutDashboard }, { href: "/products", label: "Product Library", icon: Package }, { href: "/products/import", label: "Import Products", icon: Upload }, { href: "/settings", label: "Settings", icon: Settings }]; return <div className="flex min-h-screen"><aside className="hidden w-60 shrink-0 border-r border-slate-200 bg-white p-4 md:block"><Link href="/dashboard" className="mb-8 block px-3 text-lg font-bold tracking-tight">RFQ <span className="text-blue-600">Copilot</span></Link><nav className="space-y-1">{links.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className={`flex items-center gap-3 rounded-md px-3 py-2 text-sm font-medium ${path === href ? "bg-blue-50 text-blue-700" : "text-slate-600 hover:bg-slate-50"}`}><Icon size={17} />{label}</Link>)}</nav><button onClick={() => createClient().auth.signOut().then(() => window.location.href = "/login")} className="mt-8 flex items-center gap-3 px-3 py-2 text-sm text-slate-500"><LogOut size={17} />Log out</button></aside><div className="min-w-0 flex-1"><header className="flex h-16 items-center justify-between border-b border-slate-200 bg-white px-6"><span className="text-sm font-semibold md:hidden">RFQ Copilot</span><span className="ml-auto text-xs text-slate-400">Workspace</span></header><main className="mx-auto max-w-7xl p-6">{children}</main></div></div> }
