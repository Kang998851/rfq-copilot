"use client";
import Link from "next/link"; import { useEffect, useState } from "react"; import { createClient } from "@/lib/supabase/client"; import type { Product } from "@/types/database";
export default function ProductLibrary() { const [products, setProducts] = useState<Product[]>([]); const [search, setSearch] = useState(""); const [loading, setLoading] = useState(true); const load = async () => { const { data } = await createClient().from("products").select("*").order("created_at", { ascending: false }); setProducts((data ?? []) as Product[]); setLoading(false); }; useEffect(() => { load(); }, []); const filtered = products.filter(p => [p.sku, p.name, p.model].some(v => (v ?? "").toLowerCase().includes(search.toLowerCase()))); async function toggle(product: Product) { await createClient().from("products").update({ active: !product.active }).eq("id", product.id); setProducts(ps => ps.map(p => p.id === product.id ? { ...p, active: !p.active } : p)); } return <div><div className="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="label">Catalog</p><h1 className="mt-2 text-2xl font-bold">Product Library</h1><p className="mt-1 text-sm text-slate-500">{products.length} products in your private workspace.</p></div><Link href="/products/import" className="btn-primary">Import products</Link></div><div className="mb-4 rounded-lg border border-slate-200 bg-white p-3"><input className="w-full text-sm outline-none" placeholder="Search SKU, name or model…" value={search} onChange={e => setSearch(e.target.value)} /></div><div className="overflow-x-auto rounded-lg border border-slate-200 bg-white">{loading ? <div className="p-8 text-sm text-slate-500">Loading product library…</div> : <table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{["SKU", "Product Name", "Model", "Category", "Material", "Size", "Cost", "MOQ", "Lead Time", "Status"].map(h => <th key={h} className="px-4 py-3 font-semibold">{h}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{filtered.map(p => <tr key={p.id} className="hover:bg-slate-50"><td className="px-4 py-3 font-mono text-xs"><Link className="text-blue-600 hover:underline" href={`/products/${p.id}`}>{p.sku}</Link></td><td className="px-4 py-3 font-medium">{p.name}</td><td className="px-4 py-3 text-slate-600">{p.model || "—"}</td><td className="px-4 py-3 text-slate-600">{p.category || "—"}</td><td className="px-4 py-3 text-slate-600">{p.material || "—"}</td><td className="px-4 py-3 text-slate-600">{p.size || "—"}</td><td className="px-4 py-3">{p.cost == null ? "—" : `${p.cost} ${p.currency}`}</td><td className="px-4 py-3">{p.moq ?? "—"}</td><td className="px-4 py-3">{p.lead_time_days ? `${p.lead_time_days} days` : "—"}</td><td className="px-4 py-3"><button onClick={() => toggle(p)} className={`rounded-full px-2.5 py-1 text-xs font-semibold ${p.active ? "bg-green-50 text-green-700" : "bg-slate-100 text-slate-500"}`}>{p.active ? "Active" : "Disabled"}</button></td></tr>)}</tbody></table>}{!loading && filtered.length === 0 && <div className="p-10 text-center text-sm text-slate-500">No products match your search.</div>}</div></div> }
