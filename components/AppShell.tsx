"use client";
import Link from "next/link"; import { usePathname } from "next/navigation"; import { LayoutDashboard, Package, Upload, Settings, LogOut, FileText } from "lucide-react"; import { createClient } from "@/lib/supabase/client";
export default function AppShell({ children }: { children: React.ReactNode }) { const path = usePathname(); const links = [{ href: "/dashboard", label: "Overview", icon: LayoutDashboard }, { href: "/rfqs", label: "RFQs", icon: FileText }, { href: "/products", label: "Products", icon: Package }, { href: "/products/import", label: "Imports", icon: Upload }, { href: "/settings", label: "Settings", icon: Settings }]; return <div className="flex min-h-screen"><aside className="hidden w-60 shrink-0 border-r border-slate-200 bg-white p-4 md:flex md:flex-col"><Link href="/dashboard" className="mb-8 block px-3 text-lg font-bold tracking-tight">RFQ <span className="text-blue-600">Copilot</span></Link><nav className="space-y-1">{links.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className={`flex items-center gap-3 rounded-md px-3 py-2 text-sm font-medium ${path === href ? "bg-blue-50 text-blue-700" : "text-slate-600 hover:bg-slate-50"}`}><Icon size={17} />{label}</Link>)}</nav><div className="mt-auto border-t border-slate-100 pt-4"><p className="px-3 text-xs text-slate-400">Company workspace</p><button onClick={() => createClient().auth.signOut().then(() => window.location.href = "/login")} className="mt-2 flex items-center gap-3 px-3 py-2 text-sm text-slate-500"><LogOut size={17} />Log out</button></div></aside><div className="min-w-0 flex-1"><header className="flex h-16 items-center justify-between border-b border-slate-200 bg-white px-6"><span className="text-sm font-semibold md:hidden">RFQ Copilot</span><div className="ml-auto flex items-center gap-3"><span className="hidden text-xs text-slate-400 sm:inline">Workspace</span><button onClick={() => createClient().auth.signOut().then(() => window.location.href = "/login")} className="text-xs font-semibold text-slate-500 md:hidden">Log out</button></div></header><main className="mx-auto max-w-7xl p-6">{children}</main></div></div> }
