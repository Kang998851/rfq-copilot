import Link from "next/link"; import { notFound } from "next/navigation"; import { AlertCircle, CheckCircle2 } from "lucide-react"; import { demoRfqs } from "@/lib/demo/data";
export function generateStaticParams() { return demoRfqs.map(r => ({ id: r.id })); }
export default async function DemoRfqDetail({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const rfq = demoRfqs.find(r => r.id === id); if (!rfq) notFound(); return <div className="max-w-5xl"><Link href="/demo/rfqs" className="text-sm text-blue-600">← Sample RFQs</Link><div className="mb-7 mt-4 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="label">Sample RFQ · {rfq.id}</p><h1 className="mt-2 text-3xl font-bold">{rfq.buyer}</h1><p className="mt-2 text-sm text-slate-500">Received {rfq.received} · Source {rfq.source}</p></div><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-700">DEMO DATASET</span></div><div className="grid gap-4 md:grid-cols-4"><Metric label="Items" value={rfq.items} /><Metric label="Quantity" value={rfq.quantity} /><Metric label="Match confidence" value={`${rfq.confidence}%`} /><Metric label="Review status" value={rfq.status} /></div><div className="mt-6 grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><div className="border border-slate-200 bg-white p-6"><p className="label">Requirement review</p><h2 className="mt-3 text-lg font-bold">Customer requirement</h2><div className="mt-4 rounded-md bg-slate-50 p-4 text-sm font-medium leading-7">{rfq.requirement}<br />Quantity: {rfq.quantity}</div><h2 className="mt-7 text-lg font-bold">Matched product</h2><div className="mt-4 flex items-start justify-between rounded-md border border-green-200 bg-green-50 p-4"><div><p className="text-sm font-semibold text-green-900">{rfq.matched}</p><p className="mt-1 text-xs text-green-700">Confidence score {rfq.confidence}%</p></div><CheckCircle2 className="text-green-700" size={20} /></div></div><div className="border border-slate-200 bg-white p-6"><p className="label">Human review queue</p><h2 className="mt-3 text-lg font-bold">Missing information</h2><div className="mt-4 space-y-3">{rfq.missing.map(item => <div key={item} className="flex items-center gap-3 rounded-md border border-amber-200 bg-amber-50 p-3 text-sm text-amber-900"><AlertCircle size={17} />{item}</div>)}</div><p className="mt-6 text-xs leading-5 text-slate-500">Sample RFQ for product demonstration. These values are fixed demo data and do not represent real-time AI extraction.</p></div></div></div> }
function Metric({ label, value }: { label: string; value: string | number }) { return <div className="border border-slate-200 bg-white p-4"><p className="label">{label}</p><p className="mt-2 text-lg font-bold">{value}</p></div> }
