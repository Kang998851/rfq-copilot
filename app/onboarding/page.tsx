"use client";
import { FormEvent, useState } from "react"; import { createClient } from "@/lib/supabase/client";
export default function Onboarding() { const [form, setForm] = useState({ name: "", country: "", industry: "", website: "", currency: "USD" }); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const update = (key: string, value: string) => setForm(f => ({ ...f, [key]: value })); async function submit(e: FormEvent) { e.preventDefault(); setLoading(true); setError(""); const supabase = createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return void (window.location.href = "/login"); const { data, error: companyError } = await supabase.from("companies").insert(form).select().single(); if (companyError || !data) { setError(companyError?.message ?? "Could not create workspace."); setLoading(false); return; } window.location.href = "/dashboard"; setLoading(false); } return <main className="flex min-h-screen items-center justify-center p-6"><div className="w-full max-w-lg rounded-xl border border-slate-200 bg-white p-8 shadow-sm"><p className="text-sm font-bold text-blue-600">RFQ Copilot</p><h1 className="mt-5 text-2xl font-bold">Set up your company</h1><p className="mt-2 text-sm text-slate-500">This workspace keeps your product data private to your company.</p><form onSubmit={submit} className="mt-8 space-y-5">{error && <div className="rounded-md border border-red-200 bg-red-50 p-3 text-sm text-red-700">Unable to save workspace. Please try again.</div>}<Field label="Company Name" value={form.name} onChange={v => update("name", v)} required /><Field label="Country" value={form.country} onChange={v => update("country", v)} required /><Field label="Industry" value={form.industry} onChange={v => update("industry", v)} required /><Field label="Website (Optional)" value={form.website} onChange={v => update("website", v)} /><div><label className="label">Default Currency</label><select className="field" value={form.currency} onChange={e => update("currency", e.target.value)}><option>USD</option><option>EUR</option><option>GBP</option><option>CNY</option></select></div><button className="btn-primary w-full" disabled={loading}>{loading ? "Creating workspace…" : "Continue to dashboard"}</button></form></div></main> }
function Field({ label, value, onChange, required }: { label: string; value: string; onChange: (v: string) => void; required?: boolean }) { return <div><label className="label">{label}</label><input className="field" value={value} onChange={e => onChange(e.target.value)} required={required} /></div> }
